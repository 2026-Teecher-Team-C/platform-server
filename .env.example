# 운영(EC2) 배포 시 infra/.env 로 복사해 채운다. 실제 값은 절대 커밋하지 않는다 (public 레포).
DB_URL=jdbc:postgresql://<rds-endpoint>:5432/platform
DB_USERNAME=
DB_PASSWORD=
# 이후 단계에서 추가될 값: REDIS_URL, QUARANTINE_BUCKET, SLACK_WEBHOOK_URL
